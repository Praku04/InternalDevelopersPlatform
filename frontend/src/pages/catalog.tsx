// Placeholder catalog landing page. Per-service forms (Section 6 / Flow A)
// land in Phase 5 once the EC2 end-to-end flow is built.
export default function Catalog() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-slate-900">Infrastructure Catalog</h1>
      <p className="mt-2 text-slate-600">Self-service provisioning forms (VPC, EC2, ALB, S3, RDS) land here in Phase 5.</p>
    </div>
  );
}
