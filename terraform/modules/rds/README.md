# RDS Module (Planned)

Not yet implemented. Scheduled per the repository's phased build plan —
the RDS module will be added when the platform's module-generation flow
first needs it, following the same pattern as the other approved modules
(mandatory encryption, private subnet only, deletion protection in prod,
no public accessibility).
